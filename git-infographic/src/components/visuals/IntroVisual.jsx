import React from 'react';

const IntroVisual = ({ currentTime, duration, isPlaying }) => {
    return (
        <div style={{
            width: '100%',
            height: '100%',
            display: 'flex',
            justifyContent: 'center',
            alignItems: 'center',
            color: '#fff',
            fontSize: '1.2rem',
            border: '1px dashed #30363d',
            borderRadius: '8px'
        }}>
            Intro Visual (Blank)
        </div>
    );
};

export default IntroVisual;
